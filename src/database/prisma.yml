datamodel: datamodel.graphql
endpoint: https://localhost:4466/the-bid-auctions/local
secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
    - echo "Deployment finished"
    - graphql get-schema --project database
    - graphql prepare
